import 'package:flutter/material.dart';

class PsalmPage extends StatelessWidget {
  const PsalmPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Psalm 1"),
      ),
    );
  }
}
